package com.example.timerush.utils

import com.example.timerush.domain.model.LeaderboardUser

object LeaderboardData {

    val users = listOf(
        LeaderboardUser("You", 40),
        LeaderboardUser("Alex", 60),
        LeaderboardUser("Riya", 50)
    )
}
